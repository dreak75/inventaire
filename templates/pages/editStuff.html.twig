{# empty Twig template #}

{% extends 'base.html.twig' %}

{% block body %}
<div class="container mt-4">
<h1>Editer l'affaire</h1>

{{ form_start(form) }}
	{{ form_rest(form) }}
	<button class="btn btn-primary">Modifier</button>
{{ form_end(form) }}
</div>

{%  endblock %}